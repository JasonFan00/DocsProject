/**
 * Represents the site structure of the generated pages (url to page, page title, etc.), which will then be used in generating the home page with thyme leaf.  Will also support nested categories.
 */
/**
 * @author Jason Fan
 *
 */
package com.student.application.structure;